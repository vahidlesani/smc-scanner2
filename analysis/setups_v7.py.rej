diff a/analysis/setups_v7.py b/analysis/setups_v7.py	(rejected hunks)
@@ -1039,6 +1039,8 @@ def scan_setups(bundle: MarketBundle, style: str) -> List[SignalCandidate]:
                     try:
                         from analysis.zone_context import enrich_candidate_zone_context
                         enrich_candidate_zone_context(bundle, result, style)
+                        from analysis.zone_policy import attach_zone_policy
+                        attach_zone_policy(result)
                     except Exception as exc:  # zone labels are diagnostic only
                         result.metadata["zone_context_error"] = str(exc)[:160]
                 candidates.append(result)
