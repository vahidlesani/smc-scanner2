# کارتابل اجرا — Viva 09-19/20 (دسته‌بندی پیام اصلاحی + چارت‌ها)
> **ممیزی فاز اول ۰۹-۲۳:** قانون strictly-after-alert، True Range ATR، clamp هندسهٔ trendline، NameErrorهای risk/spot، و CI pytest کامل اصلاح و با **۳۷۵ تست سبز + ۷ تست وب‌اپ** راستی‌آزمایی شد. Railway هنوز دیپلوی نشده است.
> **فاز دوم ۰۹-۲۳:** قرارداد ماهیت و جهت شکست برای edgeهای Trend/Pattern کامل شد؛ relabel پرچم‌ها جهت stale تولید نمی‌کند و confirmation contract جلوی معکوس‌کردن جهت شکست را می‌گیرد. **۳۷۶ تست سبز + ۱ اسکیپ**.
> **فاز سوم ۰۹-۲۳:** internal فقط channel/range، geometry منتشرشده به ازای signal ID immutable، سیگنال‌های جدید بدون snapshot قبلی، و حذف label/pill کنار ابزار. **۳۷۷ تست سبز + ۱ اسکیپ**.
منبع: پیام دسته‌بندی‌شدهٔ Viva + ۱۵ چارت اصلاحی/توضیحی. وضعیت‌ها: ✅ انجام این دور | ⏳ P1 دور بعد | 🕰 منتظر حکم Viva.

## الف) مدیریت سرمایه / نردبان خروج
1. ✅ نردبان v3: شکل ابزار دقیقاً مثل قبل (۵ پیل TP)، بدون کف 1R (TP1 و استاپ هم‌اندازه نیستند)، خروج‌ها ۴۰/۳۰/۳۰ روی TP1..TP3 و TP4/TP5 فقط اطلاع‌رسانی (پیل INFO)؛ BE خالص + کف‌های حفاظتی + تریل + خروج هوشمند سر جایشان.
2. 🕰 تعداد نهایی TPها (شاید ۴ تا) و بازنگری وزن‌ها — Viva فکر می‌کند (طراحی برای استفادهٔ عمومی/اتوترید آینده).
3. 🕰 ثبت صادقانه: با وزن ۴۰/۳۰/۳۰ روی پله‌های یک‌پنجمی، بدترین برد پس از TP1 کوچک می‌شود (مثلاً ۰٫۱۶R برای هدف ۲R)؛ کف حفاظتی/تریل جبران می‌کنند ولی قضاوت نهایی با دادهٔ ژورنال (میانگین برد/باخت پنل).

## ب) قوانین تأیید (باگ‌های چارت‌ها)
4. ✅ قانون خلاف‌جهت: برخورد به خط/ناحیه = فقط هشدار؛ تأیید نیازمند کلوز معتبر فراتر از سوینگ هم‌جهت (شکست ساختاری). دلیل: شکار سلرها/جمع‌آوری لیکوئیدیتی در برخورد.
5. ✅ تایم روزانه خلاف جهت: اول بریک ساختار زیر/بالای قیمت در ۴ساعته (کلوز)، سپس کلوز روزانه فراتر از ضلع پایین/بالای الگو (حالت ZEC).
6. ✅ گیت ناحیهٔ مخالف MTF: نزدیک سوینگ/ناحیهٔ مخالف در تایم والد (۱۵m→۱h، ۱h→۴h، ۴h→۱d) با فاصلهٔ استاندارد ۰٫۵×ATR والد → تأیید رد می‌شود (لانگ نزدیک عرضه، شورت نزدیک تقاضا).
7. ✅ sanity هندسی: rr_tp1 < 0.25 یا rr_tp2 < 0.5 → رد DEGENERATE_GEOMETRY (حالت SUI 1D با R:R 0.02/0.03 و استاپ ۶۴٪ دور).
8. ⏳ رسم ترند نزولی کوتاه‌مدت داخل روند صعودی (LTC): موتور nested باید ساب‌ترند مخالف را هم بکشد (spec §8) — کالیبراسیون انجین ترند.
9. ⏳ بازبینی کالیبراسیون انجین ترند/الگوها بر اساس spec بایگانی‌شده (pivot lag، touch clustering اتخاذ شده؛ §9 composite و §14 conflict هنوز deferred).

## ج) نمایش چارت lifecycle
10. ✅ قانون محور زمان (اجرا شد ۰۹-۲۰ + اصلاحیهٔ همان روزِ Viva: «۴۰ مثال بود»): ریشهٔ باگ = لنگر ابزار در هر رندر روی «آخرین کندل» بازنشانی می‌شد (tool_start = count-20) و لنگر زمانی الزاماً ثبت نمی‌شد. حالا: لنگر ابزار یک‌بار در time of alert ثبت می‌شود (tool_anchor_ts) + ابزار روی کندل واقعی ورود می‌نشیند (tool_entry_ts از entry_filled_at)؛ نواحی/رنج تایمِ مبدأ (ts0) را حمل می‌کنند و رندر با searchsorted روی زمان می‌نشاند. معیار = خودِ هندسهٔ ابزار، نه عدد ثابت: هر کندل بستهٔ بیرون از «ناحیهٔ رسم ابزار» (فراتر از لبهٔ زمانی ابزار = کندل ورود + ۴۲ کندل جلوتر، یا بیرون از باند قیمتی ابزار: بالای بالاترین پیل/زیر استاپ) شمرده می‌شود؛ تا وقتی صفر است و قیمت داخل ابزار لانگ/شورت است، همهٔ پیام‌های دارای چارت لایو (تحلیل/آپدیت پیش از تأیید + پس از تأیید) روی تایم تریگر می‌مانند؛ از اولین کندل بیرون‌رفته، همان ابزار یک پله بالاتر ۱۵m→۱h→۴h→۱d رندر می‌شود (شیب/کندل بریک با ضریب مقیاس ۰٫۲۵) + یادداشت یک‌خطی در پیام (داخل چارت نه) — شامل پیام‌های TP/استاپ (چارت لایو دارند) و آپدیت‌های زنده. قانون چارت=تایم تریگر فقط برای هشدار/تأیید است. چارت‌های TP/لایو/نتیجه = مجاز به ارتقای تایم (حکم Viva). فال‌بک: اگر ونو تایم بالاتر را نداد، همان تریگر بدون یادداشت (بدون stall).

## د) داده و تایم‌فریم‌ها
11. ✅ اوربیت ۳ دقیقه دارد: TF_MAP + سلسله‌مراتب مانیتور ۱۵m→۳m (و ۵m/۳m→۱m).
12. 🕰 فاز ۱ دادهٔ کمکی (ترس‌وطمع/استیبل‌کوین/کارمزد) — منتظر حکم شروع.

## هـ) عیب‌یابی خودم روی چارت‌های ارسالی
- SUI 1D LONG: استاپ روی سوینگ می (۰٫۴۷) و اهداف ~۱٪ → ابزار بی‌معنی؛ ریشه: لنگر استاپ قدیمی‌ترین سوینگ + سقف ۲۵×ATR روی 1D خیلی شل؛ fix: گیت degeneracy + (P1) بازنگری لنگر استاپ swing-recent.
- ZEC 1D SHORT: تأیید خلاف‌جهت فقط با برخورد کندل فول‌بادی به ترند بالایی؛ بدون بریک ۴ساعته/کلوز روزانه زیر ضلع پایین → gate جدید جلویش را می‌گیرد؛ ترتیب درست Viva ثبت شد.
- LTC 15m SHORT و POL 15m/1H SHORT: تأیید بدون شکست معتبر کف (حتی بدون برخورد) → gate ساختار-بریک؛ در LTC ساب‌ترند نزولی کوتاه هم رسم نشده (بند ۸).
- POL 15m تکراری در دو سیگنال هم‌زمان (K124739 دوبار) → بررسی dedup هم‌نماد چندتایم‌فریمی (P1).
- ETH 1H LONG و FIL 15m LONG و SUI 15m: هندسه/ناحیه‌ها خوب؛ FIL با نردبان جدید هم‌تراز بود (حالا با v3 پنج‌پيله می‌شود).
- ZEC T190725 vs T750613: دو سناریوی هم‌نماد با استاپ متفاوت (1,519 vs 1,555) → نیاز به قانون یکتایی سناریوی فعال per نماد/جهت (P1، مرتبط با dedup).

## و) جدول تایم‌فریم‌ها (نسخهٔ اجرا)
| مرحله | تایم تحلیل/چارت | تایم بررسی کمکی | تایم مانیتور/تریل/خروج |
|---|---|---|---|
| هشدار اولیه و چارت تحلیلی | تریگر (قانون ثابت) | والد: ۱۵m→h، ۱h→۴h، ۴h→۱d | — |
| تأیید | کلوز تریگر (+کلوز تایم الگو) | خلاف‌جهت: بریک ساختار در والد (۱d→۴h) | — |
| ورود/استاپ/TP/تریل/خروج هوشمند | — | — | سلسله‌مراتب: ۱d→۱h، ۴h/۲h/۱h→۱۵m، ۳۰m/۱۵m→m، ۵m/۳m→۱m (فالبک: تریگر) |
| رندر lifecycle | تریگر؛ سرریز زمانی → تایم بالاتر بدون جابجایی ابزار + یادداشت | | |

## دوره ۷ (۰۹-۲۰ — ثبت اجرا)
- ✅ قانون محور زمانِ بالا پیاده و با تست‌های عددی همان مثال Viva قفل شد: ۴۰ کندل = همان ۱۵ دقیقه، ۴۱ کندل = ۱ ساعته با یادداشت؛ نمونهٔ رندر واقعی: /home/user/proof_time_axis.png (پنل A لحظهٔ ثبت روی ۱۵m، پنل B صدها کندل بعد روی ۱h با همان ابزار).
- ✅ ناهماهنگی آستانهٔ خروج اضطراری رفع شد: SMART_EXIT_RED=2 / ORANGE=1 (کد/داکیومنت/تست با حکم «۲ نشانه = کلوز فوری» هم‌راستا شد؛ runtime از قبل ۲ را می‌بست).
- ✅ CI: نصب pytest در ورک‌فلوی GitHub (یافتهٔ P1 هر دو فایل بازبینی) — اجرای تازه دیگر ModuleNotFoundError نمی‌دهد.
- ⏳ از فایل‌های بازبینی (P0/P1 باقی‌مانده، به‌ترتیب): (الف) پروفایل رجیستری واحد Live/Backtest + ریپلی confirm_tf واقعی [ریسک فقط روی اعتبار بک‌تست، مسیر لایو مستقل]؛ (ب) لاگ ساختاریافته به‌جای except خاموش؛ (پ) قرنطینهٔ analysis/structure.py+mtf.py (سوئینگ centered)؛ (ت) رفع deprecationهای timestamp؛ (ج) گزارش قیف per setup؛ (د) shadow mode برای TechnoClassic/Viva-TLBreak.
- 🕰 «کیفیت خروج اضطراری برای حفظ سود» — Viva بعد بررسی می‌کند؛ ما بستهٔ شواهد ژورنال را آماده می‌کنیم (نرخ نشانه‌ها، قیمت کلوز در برابر کندل بعد، R حفظ‌شده در برابر R ازدست‌رفته) و تا حکم او چیزی تغییر نمی‌کند.

## دوره ۶ (ثبت اجرا)
- ✅ خروج فوری فاز حفاظت با ۲ نشانه (کلوز در همان قیمت، قبل از برگشت به TP1)؛ ۱ نشانه = هشدار کوتاه.
- ✅ تایم نشانه‌ها/تأیید ۱۵دقیقه = ۵ دقیقه (حکم Viva)؛ ۳ دقیقه در نقشهٔ venue باقی ماند.
- ✅ ریفاین محاسبات/لوریج: هزینه در ریسک مؤثر، جریمهٔ استاپ پهن، سقف لوریج برای RR<1.
- ⏳ P1: ترمز زیان روزانه مبتنی بر ژورنال؛ حکم نهایی تعداد TPها (شاید ۴ تا).

## دوره ۸ (۰۹-۲۰ — سند «مدیریت ویوا» + ۵ چارت اصلاحی)
- ✅ سقف فاصلهٔ اهداف بر پایهٔ تایم‌فریم (بند ۴ سند): روزانه ۱۰٪ · ۴ساعته ۷٪ · ۱ساعته و ۱۵دقیقه ۵٪ — هدف نهایی در لحظهٔ تأیید کلمپ می‌شود (چارت XRP: ۱٫۱۱۹ = ۱۹٪ دور → ۱٫۳۱۱۹ = ۵٪)؛ ابزار/پیل‌ها دست‌نخورده، TP1≈۱٪ و TP3 = ۳٪ برای ۱۵m.
- ✅ پیل‌های وزنه‌صفر دیگر خروج محسوب نمی‌شوند: یک کندل که TP4/TP5 را هم لمس کند، فقط TP1..TP3 را رسید می‌زند و بسته می‌شود (بدون رسید اضافه).
- ✅ توالی نارنجی→قرمز طبق بند ۶٫۱: نشانهٔ اول در تایم سریع (۱۵m→۳m، ۱h→۵m، ۱d→۱۵m) هشدار نارنجی می‌دهد؛ و یک «پین‌بار معکوس بسته‌شده» در تایم مانیتور به‌تنهایی هم باقی‌مانده را می‌بندد (قرمز).
- ✅ پروفایل «مدیریت ویوا» جداگانه و قابل‌سوئیچ: مارجین ثابت بر اساس قیمت (۳۰/۴۰/۵۰ دلار) + اهرم ۲۰× — پیش‌فرض خاموش (`VIVA_MANAGEMENT_PROFILE`)، مدیریت استاندارد دست‌نخورده. هشدار لیکوئید (فاصلهٔ ۵٪ در ۲۰×) به‌جای تغییر خودکار حجم.
- ✅ پاک‌سازی چارت‌ها: باکس measured-move روی چارت‌های تأییدشده دیگر در ناحیهٔ استاپ کشیده نمی‌شود؛ خطوط ترندِ تقریباً یکسان در یک چارت ادغام می‌شوند (سقف ۲ والد + ۱ فرزند در هر سمت)؛ لنگر زمانی رنج/نواحی (بند ۱۰).
- 🕰 حکم لازم (تفسیر سند): (الف) روشن‌کردن پروفایل مدیریت ویوا؟ (ب) قانون «خوشهٔ TP1..TP3»: با سقف ۵٪ و گیت TP1R 1.5–2.5، TP3 دقیقاً روی سقف می‌افتد — آیا TP2/TP3 نزدیک‌تر و خوشه‌ای شوند؟ (ج) ورود مجدد پس از پولبک (بند ۶٫۲) به‌عنوان یک قابلیت جدید ساخته شود؟ (د) گیت rr≥1.5/2.2 به‌عنوان فیلتر ورود بماند یا با سقف تایم‌فریم جایگزین شود؟
- 🕰 ابهامات سند (بند ۱۳) علامت‌گذاری شد و تفسیر جدیدی اضافه نشد: تعریف بیس معتبر، الگوریتم پین/انگالف/دوجی، کدام کلوز اولین پین را اعتبارسنجی می‌کند، متن دقیق نارنجی/قرمز، تشخیص پولبک از برگشت روند، نسبت تقسیم ۵گانه، هدف نهایی شورت (کف قبلی یا بعدی)، نمادهای دقیقاً روی مرز ۵/۵۰ دلار، اولویت نواحی بین تایم‌فریم‌ها، رفتار گپ/جهش.
- ✅ فایل سند به `handoff/specs/VIVA-MANAGEMENT-SPEC-v1.0.md` بایگانی و از uploads حذف شد.

## دوره ۹ (۰۹-۲۰ — «مدیریت ویوا روشن»، بازنویسی TP، ورود مجدد، ورود داخلی)
- ✅ «مدیریت ویوا» اعمال شد (تا اطلاع ثانوی): <۵$ ۳۰$ · ۵ تا <۵۰$ ۴۰$ · ≥۵۰$ ۵۰$ — اهرم ۲۰× — با کلید `VIVA_MANAGEMENT_PROFILE`؛ «مدیریت سرمایه استاندارد» دست‌نخورده و قابل‌بازگشت. هر دو با نام دقیق در `handoff/specs/CAPITAL-MANAGEMENT-PROFILES-v1.0.md` مستند شدند (هرگز مخلوط نمی‌شوند).
- ✅ گیت R:R (۱:۱٫۵ و ۱:۲٫۲) بازنشسته شد — R:R فقط گزارش می‌شود؛ صحت هندسه با معیار مطلق (ATR/درصد) بررسی می‌شود.
- ✅ اهداف ساختاری تایم تریگر + نواحی تایم بالاتر؛ سقف فاصله: روزانه ۱۰٪ · ۴ساعته ۵–۷٪ · ۱h/۱۵m ۳–۵٪.
- ✅ باگ گپ TP1→TP2 رفع شد: تقسیم پنج‌گانهٔ یکنواخت + سقف ۱٫۳ برابر گام؛ پیل پنجم = سقف/کف ساختاری کلمپ‌شده؛ ۴۰/۳۰/۳۰ دست‌نخورده.
- ✅ ورود مجدد پس از TP1: اولین پین‌بار معکوسِ بسته‌شده در تایم سریع (۵m/۳m) = خروج فوری باقی‌مانده؛ پول‌بک معتبر = سیگنال 🔁 ورود مجدد (سود اولیه قفل).
- ✅ لِین ورود داخلی: لانگ فقط از کف کانال/رنج با تأیید کندل، استاپ پشت کانال با بافر، اهداف زیر سقف کانال (شورت آینه).
- ✅ پوشش تست: `tests/test_round9_rules.py` (۵ تست) + به‌روزرسانی تست‌های مدیریت استاندارد با پین‌کردن پرچم پروفایل. مجموع: ۱۷۹ پاس / ۱ اسکیپ.
- ⏳ در انتظار حکم: تعداد نهایی TPها (شاید ۴)؛ حکم خروج اضطراری؛ فاز ۱ حجم/آنچین.

## دوره ۱۰ (۰۹-۲۰ — دکترین مسیر هدف + پاک‌سازی ترندها)
- ✅ لدر = مسیر قیمتی یکنواخت: ورود→سطح معتبر (یا ضلع مقابل الگو یا نُرم تایم) به ۵ قسمت مساوی؛ خروج TP1..TP3 (۴۰/۳۰/۳۰) = ۲۰/۴۰/۶۰٪ مسیر؛ اسنپ TP1 به سطح فقط در ±۲۰٪ گام. (نمونه: BNB شورت قبلاً ۰٫۱۸٪/۰٫۳۵٪/۰٫۵۳٪ بود → حالا ۱٪/۲٪/۳٪ روی مسیر ۵٪.)
- ✅ حذف کامل R:R از چارت، پیام تأیید، یادداشت AI و راهنما (چارت‌های ارسالی R:R منفی نشان می‌دادند).
- ✅ ورود داخلی: ورود فقط از لبه با تأیید کندل، استاپ پشت ضلع کانال + آخرین سویینگ + بافر، هدف = ضلع مقابل، خروج‌ها زیر/بالای ضلع مقابل.
- ✅ گیت containment برای همهٔ الگوهای دوخطی (Broadening هم پوشش داده شد؛ چارت WLD از همین شکاف رد شده بود).
- ✅ ترندها: جریمهٔ فاصله در امتیاز خط + حذف خط متناقض با جهت معامله (حذف «ترند قرمز لنگ در هوا» در BNB، برجسته‌شدن خط نزولیِ درست).
- 🕰 سه پرسش حکمی: مسیر بدون سطح = ۵٪ یا ۳٪؟ فلور استاپ شکست‌ها؟ TP1 = یک‌پنجم مسیر یا اولین سطح معتبر؟

## دوره ۱۱ (۰۹-۲۰ — بدون ATR، باند ۳–۵٪ بسته به موقعیت، همهٔ ستاپ‌ها)
- ✅ استاپ: بافر ۰٫۱۰٪ یا ۵ تیک (بدون ATR) و لنگر = آخرین سوینگ؛ BNB از ۲٫۳۲٪ به ۰٫۵۵٪ رسید.
- ✅ مسیر هدف: سطح معتبر تایم/تایم بالاتر → همان فاصله؛ وگرنه ۳–۵٪ بسته به فاصله تا سقف/کف قبلی؛ تقسیم ۵گانه یکنواخت؛ TP1=۱/۵ و خروج ۴۰/۳۰/۳۰.
- ✅ داخل الگو: مسیر = فاصله تا ضلع مقابل؛ خروج‌ها زیر/بالای ضلع؛ خروج اضطراری طبق قانون قبلی.
- ✅ اعمال روی همهٔ ستاپ‌ها: ALBROX، TLBREAK/VIVA-TLBREAK، TECHCLASSIC، PINWALLQ، PINVAL — گیت‌های R:R این ستاپ‌ها هم حذف شد.
- 🕰 باقی‌ماندهٔ حکمی: مقدار دقیق بافر اگر ۱٪ نمی‌پسندد؛ تعداد نهایی TP (شاید ۴)؛ حکم خروج اضطراری؛ فاز ۱ حجم/آنچین.

## دوره ۱۲ (۰۹-۲۱ — جهت شکست + استاپ صادقانه)
- ✅ گیت جهت شکست روی همهٔ ستاپ‌ها: بعد از شکست و کلوز، پوزیشن فقط در جهت ضلع شکسته تأیید می‌شود (شورت بعد از شکست صعودی/لانگ بعد از شکست نزولی رد می‌شود) — خطوط مرده (دورتر از ۳×ATR) کار نمی‌کنند.
- ✅ استاپ PINVAL و TLBREAK اصلاح شد: «پشت آخرین سویینگ با بافر» (حذف بیشینهٔ ۶ پیوت و حذف دوردست‌گیری min/max).
- ✅ سقف افق استاپ: استاپ بزرگ‌تر از فاصلهٔ مجاز تی‌پی‌ها (۱۵m ۵٪ · ۴h ۷٪ · ۱d ۱۰٪) دیگر منتشر نمی‌شود.
- 🕰 پرسش: سناریوی با استاپ دور، حذف کامل یا انتشار به‌عنوان «تحلیل بدون ورود»؟

### دورهٔ ۱۶ فاز ۲ (۰۹-۲۲ — جداسازی کامل اسپات + قوانین فرمت ویوا · کامیت `c876965` روی شاخه)
- ✅ جداسازی کامل («هیچ ارتباطی بین ستاپ‌های فیوچرز و اسپات نباید وجود داشته باشه»): کد ستاپ اسپات = **SPOTBREAK** (دیگر TLBREAK/TECHCLASSIC قفل نمی‌شود؛ لایسنس/زنجیره/آمار جدا)؛ تریگرها = ست خودش: **4h · 8h · 12h · 1d · 3d** (هفتگی حذف؛ 8h/12h نیتو از Bybit)؛ شناسه یکتا = **VIVA-SPOT-E000000** (شمارندهٔ KV)؛ سقف استاپ اسپات = **۱۰٪ قانون خودش** (جدول فیوچرز دیگر روی اسپات اثری ندارد).
- ✅ لگاریتم همهٔ چارت‌ها (پاسخ به سؤال Viva: بله با گارد): `chart_log_all` پیش‌فرض روشن؛ دامنهٔ <۳٪ → خطی (تیک تمیزتر)؛ اسپات همیشه لگاریتم.
- ✅ چارت (بازبینی چشمی رندر واقعی): «یک‌سوم چپ خالی» = ردیف‌های NaN اول فریم → dropna؛ «خط طوسی روی کندل لایو» = خطوط RANGE تمام-عرض → حذف شد (گزارش گوشه می‌ماند)؛ لنگر ابزار لانگ/شورت روی چارت تأیید = **کندل لایو لحظهٔ تأیید** (حداکثر ۲ کندل داخل ابزار)؛ لیبل گوشهٔ `VIVA-SPOT-MON · SPOT`.
- ✅ پیام اسپات: لیبل **VIVA-SPOT-MON** + عدد ابطال پوزیشن و حداکثر ضرر (سقف ۱۰٪) در پیام تأیید؛ هشدارها دلایل دوخطی (معنی الگو + شاهد حجم از کندل‌ها) با قالب همان خانواده؛ خط چین FIRST STOP پشت ساختار سر جایش. پیام‌ها/چارت‌های ۵ ستاپ فیوچرز **صفر** تغییر.
- ✅ شاهد حجم صادقانه (نسبت به میانگین ۲۰کندله) در هشدارها؛ دفتر سفارشات/OI واقعی از ریلوی ۴۰۳ → نیازمند تصمیم VPS/سرویس پولی (ثبت در تصمیم‌های باز).
- ✅ تست: ۶ تست جدید فاز ۲ + به‌روزرسانی ۲ تست قدیمی به قوانین اسپات جدید → مجموع **۳۴۶ پاس / ۱ اسکیپ**؛ رندر کنترلی چشمی قبل/بعد؛ pyflakes تمیز؛ شاخه پوش شد.
- 🕰 در انتظار حکم: (الف) تریگر روزانهٔ ۱d در فیوچرز بماند؟ (پیشنهاد: بماند — هزینه ندارد) (ب) OI/دفتر سفارشات: VPS یا سرویس پولی؟

### دورهٔ ۱۶ فاز ۱ (۰۹-۲۲ — لَدر هشدار اسپات، اجرا روی شاخهٔ `round16-spot-alerts` · کامیت `2546437`)
- ✅ لَدر هشدار ۴رویدادی در `analysis/spot_engine.py`: TOUCH (برخورد اولیهٔ فتیله به هر ضلع) → NEAR_BREAK (کلوز چسبیده به ضلع، ≤۰٫۳ATR) → BREAK_DOWN (کلوز نزولی معتبر زیر ضلع پایین = فقط هشدار) → CONFIRM (همان قانون یک‌کلوز صعودی، مسیر قبلی). طبقه‌بند خالص `_stage_for_pattern` + اسکن `scan_spot_alerts` روی همان فریم‌های بدون دانلود اضافه.
- ✅ گیت ضدتکرار سه‌مرحله‌ای در KV (`spot_alerts`): ارتقای مرحله همیشه اجازه دارد؛ همان مرحله فقط بعد از کول‌داون (TOUCH ۲۴h · NEAR_BREAK ۱۲h) و کندل جدید؛ مارکر فقط **بعد از** ارسال موفق (`spot_alert_check/commit` — قانون F17/handoff). سیگنال منتشرشده لَدر آن شکل را برای همیشه می‌بندد (`spot_alert_mark_confirmed`).
- ✅ بودجهٔ مستقل هشدارها: `SPOT_ALERT_MAX_PER_DAY` (پیش‌فرض ۸) — جدا از بودجهٔ سیگنال؛ ترتیب انتشار: قوی‌ترین مرحله اول.
- ✅ باکس CryptoCove از «اولین هشدار»: باکس سبز تا «سقف بعدی ساختاری +۱٪» (`spot_box_top` از `_structural_high_above` با فیلتر برجستگی ۱٪) — در هر دو مسیر رندر (دوخطی و تک‌خطی). چارت هشدار: محور لگاریتمی (`log_scale`) + خطوط الگو (`render_patterns`) + بدون ابزار لانگ/شورت (ابزار فقط روی confirmed).
- ✅ پیام هشدار مختصر (`send_spot_alert`): عنوان مرحله، نماد·تایم·نام فارسی الگو، ضلع و فاصله٪، یک خط قانون، پانویس «تأیید فقط صعودی با کلوز معتبر».
- ✅ تست: `tests/test_round16_spot_alerts.py` (۱۲ تست) — طبقه‌بند مراحل، گیت، سقف ساختاری، متادیتای کاندید، اسکن یکپارچه. مجموع: **۳۳۸ پاس / ۱ اسکیپ**. pyflakes چهار فایل تمیز.
- ⏳ باقی فاز ۱۶: کالیبراسیون موتور الگو/ترند با ۶ نمونهٔ مرجع Viva (SOMI 3D · MANTRA 1D · ZKP 1D · BTC 4h · TAO 1W · RENDER 3D) روی فضای لگاریتمی؛ سپس پوش و دیپلوی با تأیید Viva.

### دورهٔ ۱۲ — پاس دوم (تور سخت هندسه)
- ✅ «تور سخت» روی خروجی همهٔ دیتکتورها: استاپ سمت اشتباه، استاپ دورتر از افق تایم‌فریم، تی‌پی سمت اشتباه یا بیرون افق → انتشار متوقف + لاگ `🧱 SANITY_REJECT`.
- ✅ ورود پیام = همان ورودی که استاپ/تارگت از آن حساب شده (باگ «استاپ بالای ورود» در تکنوکلاسیک رفع شد).
- ✅ تارگت پروجکشن (measured move) داخل باند تایم‌فریم بریده می‌شود (مورد ۳۴٪ DASH)؛ فِید = دیوار مقابل الگو.
- ✅ چک مجدد در لحظهٔ تأیید (`STOP_WRONG_SIDE`).
- 📊 پروب ۲۰ نماد: بیشترین استاپ ۴٫۹٪ · صفر تخلف؛ ENA/ATOM به‌خاطر استاپ ۹٫۴٪ و ۵٫۹٪ منتشر نشدند. تست‌ها ۲۰۷ پاس / ۱ اسکیپ.
- ❓ پرسش: نماد با شکست مخالف در دو تایم‌فریم → فقط سمت تایم‌فریم بالا منتشر شود؟

### دورهٔ ۱۲ — ساعت‌ها و کندل در جریان
- ✅ هر هشدار/آپدیت/Confirmed حالا «کندل مبدا + زمان شناسایی + زمان ارسال + فاصله» را نشان می‌دهد.
- ✅ ⚠️ تأخیر بیش از ۱۵ دقیقه در متن؛ بیش از دو کندل → فقط یادداشت تحلیلی، نه هشدار ورود.
- ✅ چارت: کندل خط‌چین `FORMING` + پیل `LIVE قیمت • ساعت UTC` → مشکل «گذشته مارکت» حل شد.
- 📌 علت مورد GRAM 1h: خرابی حلقهٔ ری‌استارت (۲۲:۰۰ تا ۲۲:۲۴) اسکن آن کندل را خورد؛ پس از ریکاوری ۳۹ دقیقه دیرتر منتشر شد. با سوپروایزر + هارتبیت دیگر تکرار نمی‌شود.

### دورهٔ ۱۲ — پاس سوم (حکم سقف استاپ)
- ✅ استاپ: سقف ۱٫۲۵٪ قیمت در همهٔ تایم‌فریم‌ها؛ سویینگ دور → برش استاپ، نه حذف سناریو (VVV: ۱۵٫۱۹۳ → ۱۸٫۵۶).
- ✅ تی‌پی: تلورانس ۲۰٪ روی سقف/کف اعلامی در گیت‌ها (محاسبهٔ نردبان دست‌نخورده).
- ✅ سناریوی دور از ناحیه (> ۲ ATR): بسته می‌شود (`OUT_OF_REACH`) — دیگر ۵۱ آپدیت و انتظار دو‌روزه نداریم؛ سقف ۱۲ آپدیت پایان‌کندل.
- ✅ «⚡ عبور در لحظه» فقط روی عبور واقعی؛ قانون شکست سریع محدود به ≤ ۱٫۵ ATR.
- ✅ باگ ساعت: تاریخ‌ها اضافه شد؛ «شناسایی» = زودترین مهر (رفع ترتیب معکوس شناسایی/ارسال).

### دورهٔ ۱۳ — کندل لایو و زنده‌بودن تایم‌فریم‌های بالا
- ✅ کندل لایو = کندل عادی (بدون خط‌چین/FORMING) در همهٔ چارت‌ها؛ پیل LIVE با قیمت و ساعت.
- ✅ کلید کش رندر شامل قیمت لایو شد ⇒ هیچ چارتی برای طول یک کندل یخ نمی‌زند.
- ✅ دروازهٔ پنجرهٔ کندل باز شد (۱۵m ۰–۵ دقیقه، ۱h ۰–۱۱، ۴h ۰–۱۹، ۱d ۰۰:۰۰–۰۰:۴۴) ⇒ تأیید/آپدیت/هارت‌بیت تایم‌های بالا دیگر قربانی ریستارت یا چهار دقیقهٔ باریک نمی‌شود.
- ✅ قیمت لحظه‌ای از تیکر برای هر زنجیره هر سایکل ⇒ ابطال/نزدیکی/OUT_OF_REACH روی ۴h و ۱d هم زنده.
- ✅ تأییدِ کهنه (بیش از دو کندل) = یادداشت تحلیلی، نه ورود (قانون گذشته‌بازار).
- ✅ خط «ورود مجدد» که با خطای خاموش psycopg2 از کار افتاده بود، دوباره فعال شد (الگوی LIKE پارامتری + جداسازی ردیف‌ها).

### دورهٔ ۱۴ — سقف استاپ هر تایم + تی‌پی مستقل
- ✅ سقف استاپ: ۱۵m ۱٫۲۵٪ · ۱h ۱٫۷۵٪ · ۴h ۲٫۲۵٪ · ۱d ۲٫۷۵٪ (۳۰m ۱٫۵٪ · ۲h ۲٫۰٪ — انتخاب من تا نظر او).
- ✅ ساختار نزدیک‌تر ⇒ استاپ پشت ساختار + بافر؛ دورتر ⇒ برش روی سقف (حذف نمی‌شود).
- ✅ تی‌پی هیچ وابستگی به استاپ ندارد: مسیر/۵ قسمت، فلورها پله‌ای، R:R از پیام و چارت حذف، «درصد فاصله» جایش.
- ✅ سقف روزانه تا ۱۵٪ باز شد (موقت، بازبینی فردا).
- ✅ استاپ دیگر روی خودِ ورود نمی‌افتد (مرگِ حالتِ «entry = sl» در TLBREAK/واچ): سطح ابطال از ساختارِ ۸ کندلِ آخرِ فریمِ تریگر + بافر بازسازی می‌شود و بعد سقفِ تایم‌فریم برش می‌دهد.
