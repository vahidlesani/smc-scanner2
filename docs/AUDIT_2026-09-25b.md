# ممیزی باگ ۲۰۲۶-۰۹-۲۵ (دور ب) — R31.7

دامنه: موتور الگوهای کلاسیک، موتور کندل‌ها (تک‌کندلی، خوشه‌ای، MTF)، نمایش چارت و چارت MTF، موتور روند، TLBREAK.
چارت‌ها روی **کندل‌های واقعی** بایننس بررسی شدند. نمونه‌ی OHLCV را workflow `sample-klines` در `experiments/sample_klines/` گذاشت.

ابزارهای بازتولید:
- `REPLAY_CHARTS_DIR=/tmp/charts python experiments/replay_live_setups.py …` چارت production را برای اولین N آلارم و تأیید هر ستاپ ذخیره می‌کند.
- `python experiments/chart_debug.py --at 2026-09-08T13:45 --setup TLBREAK --tf 15m` یک ستاپ را در یک لحظه‌ی تاریخی رندر می‌کند و متادیتای خطوط را هم به‌صورت JSON می‌نویسد.

## ۱) چارت‌ها («چرا چارت‌ها عجیب‌اند»)

| # | باگ | اثر روی چارت | اصلاح |
|---|---|---|---|
| C1 | زمانِ پیوت با `searchsorted` به x تبدیل می‌شد. پیوتِ قدیمی‌تر از فریم به x=0 چسبانده می‌شد. پنجره‌ی فیت ۱۷۰ کندل است ولی چارت ۹۶ تا ۱۶۴ کندل نشان می‌دهد. | کل خطِ الگو به‌اندازه‌ی شیب × تعداد کندل‌های بیرون از فریم جابه‌جا می‌شد و «در هوا» معلق می‌ماند. یک دایره‌ی پیوت هم بدون هیچ کندلی به لبه‌ی چپ می‌چسبید (BTC 1D، ۲۰۲۶-۰۹-۰۴). در TLBREAK، `polyfit` با x غلط انجام می‌شد و شیب هم عوض می‌شد. | helper `_frame_x_of_ts`: بیرون از فریم x منفی (برون‌یابی) برمی‌گرداند. شیب با x واقعی محاسبه می‌شود و رسم از لبه‌ی فریم شروع می‌شود. دایره‌های x<0 رسم نمی‌شوند. |
| C6 | پیوتِ تایم پایین‌تر که داخلِ یک کندلِ تایم بالاتر است، به کندلِ **بعدی** نگاشت می‌شد. | در هر چارت با تایم بالاتر (4h روی 1D یا 15m روی 1h)، دایره‌ها یک کندل سمت راستِ فتیله‌ی واقعی می‌نشستند. | پیوت به کندلِ دربرگیرنده نگاشت می‌شود. |
| C4 | خطوطِ تکراری: خطِ اصلیِ ستاپ، ضلعِ الگوی render-kit و خطِ child همه روی همان پیوت‌ها رسم می‌شدند. | سه خطِ سبزِ تقریباً یکسان که روی یک پیوت به هم می‌رسیدند (BTC 15m، ۲۰۲۶-۰۹-۰۸). | ضلعی که با خطِ رسم‌شده ≥۲ پیوت مشترک دارد (برای child، ≥۱)، یا ≤۰.۳۵ ATR با آن فاصله دارد، دوباره رسم نمی‌شود. |
| — | خطِ TLBREAK با `tl_a_ts` و `tl_b_ts` | همان باگِ C1 | همان اصلاح |

## ۲) TLBREAK و TECHCLASSIC — شکستِ کهنه (بزرگ‌ترین باگِ کیفیت)

| # | باگ | اصلاح |
|---|---|---|
| TB1 | `assess_projected_breakout` هر کلوزی را که ≥۰.۲۵ ATR بیرونِ خط بود قبول می‌کرد. `fit_validated_line` خطِ **شکسته‌شده‌ی تاریخی** را هم برمی‌گرداند و هیچ‌جا `break_index` چک نمی‌شد. نتیجه: یک LONG روی 1D با POI در ۶۶٬۳۶۰ در حالی که قیمت ۸۱٬۲۰۰ بود (۱۸٪ فاصله، شکستِ ۱۶ روز قبل). | `breakout_is_fresh`: یکی از ۳ کلوزِ قبلیِ تریگر باید هنوز داخلِ خط باشد، و کلوز بیش از ۳ ATR از خط دور نباشد. |
| T5 | `line_price_at_time` خطِ واترِ اولین و آخرین پیوت را حساب می‌کرد، نه خطِ فیت‌شده. لاگ‌فیت را هم نادیده می‌گرفت. | خطِ فیت‌شده (خطی یا لاگ) بر حسبِ زمان برون‌یابی می‌شود. |
| P1 | در `scan_edges`، وضعیتِ `crossed` با قیمتِ لایو تعیین می‌شد. کافی بود خطی در ۵۱ کندلِ اخیر لمس شده باشد تا در هر اسکن READY یا BREAK بدهد، حتی اگر چند روز پیش شکسته بود. BREAK هم فقط «بدنه‌ی بزرگ» می‌خواست: جهتِ کندل و کلوزِ فراتر از خط چک نمی‌شد. | تازگی: یکی از ۴ کلوزِ آخرِ تایمِ الگو باید داخلِ خط باشد (`TC_FRESH_BARS`)، مگر قیمت ≤۰.۵ ATR از خط فاصله داشته باشد (ریتست). حداکثر ۱.۵ ATR فاصله (`TC_MAX_BEYOND_ATR`). BREAK کندلی در جهتِ شکست و با کلوزِ فراتر از خط می‌خواهد. |
| P6 | مقدارِ خط در اندیسِ آخرین کندلِ بسته‌ی تایمِ الگو (زمانِ **open**) محاسبه می‌شد، یعنی ۱ تا ۲ کندلِ 4h عقب‌تر از الان. | خط در لحظه‌ی لایو ارزیابی می‌شود. |
| P2 | برچسبِ الگو (H&S یا پرچم) داخلِ حلقه‌ی دو ضلع بازنویسی می‌شد و به ضلعِ دیگر و به meta نشت می‌کرد. | برای هر ضلع برچسبِ جداگانه |
| P3 | شکستِ رو به بالای خطِ triple-top یا شانه‌های H&S به‌صورت سیگنالِ LONG با نامِ «TRIPLE_TOP/H&S» اعلام می‌شد. | بعد از cross، برچسب به هندسه‌ی پایه برمی‌گردد و `formation_invalidated` ثبت می‌شود. |
| P4 | الگو با نامِ `INVERSE_HEAD_SHOULDERS` تولید می‌شد، ولی جداولِ `_ONE_NATURE` و `_ROLE9` نامِ `INV_HEAD_SHOULDERS` را داشتند. | نامِ درست به جداول اضافه شد. |
| P5 | «پرچم» بدونِ چکِ شیب و طول تشخیص داده می‌شد. یک باکسِ ۸۷ کندلی بعد از میله‌ی ۸ کندلی هم «پرچم» حساب می‌شد. | کانالِ پرچم نباید هم‌جهتِ میله باشد و طولش ≤۴۰ کندل است. |

اندازه‌گیریِ محلی روی کندلِ واقعی (۲۰ روز، BTC و SOL):
- TLBREAK: تعدادِ `out_of_reach` از **۲۱۴ به ۰** رسید. این‌ها آلارم‌های مرده‌ای بودند که فقط کانال را شلوغ می‌کردند.
- TECHCLASSIC: از **۵۰ به ۲** رسید.
- تعدادِ ترید تقریباً ثابت ماند (۲۵ → ۲۳).
- قضاوتِ نهایی با replay دور ۵ است: ۱۰ نماد × ۱۲۰ روز، مقایسه‌ی `base`/`prodsoft10` با `legacy317`/`prodsoft10_legacy317`.

## ۳) موتور روند

| # | باگ | اصلاح |
|---|---|---|
| T1 | `structure_bias` مفهومِ CHoCH نداشت. پیوت‌ها `pivot_size` کندل دیرتر تأیید می‌شوند، پس بعد از کلوز زیرِ آخرین HL تا چند کندل bias همچنان BULLISH می‌ماند. | کلوز فراتر از آخرین سوئینگِ مخالف bias را برمی‌گرداند (`choch=True`). |
| T1b | `pivots` از `>=` استفاده می‌کند، پس دو سقفِ برابر دو پیوت می‌سازند. در مقایسه‌ی این جفت، نه HH برقرار است نه LH و روند NEUTRAL می‌شد. | پیوت‌های دوقلو داخلِ `structure_bias` یکی می‌شوند. |
| T4 | در `session_name`، شرطِ NEW_YORK قبل از overlap چک می‌شد. overlap فقط ساعت ۱۲ رخ می‌داد، لندن ساعت ۱۰ تمام می‌شد و ساعت‌های ۱۷ تا ۲۱ «خارج از سشن» نمایش داده می‌شدند. | بازه‌های پیوسته‌ی UTC: آسیا ۰–۷، لندن ۷–۱۲، overlap ۱۲–۱۶، نیویورک ۱۶–۲۱ |

## ۴) موتور کندل‌ها

| # | فایل | باگ | اصلاح |
|---|---|---|---|
| 1 | trigger_patterns | شرطِ «بیس زون را لمس کرده» همیشه true بود، چون زون با خودش مقایسه می‌شد. | `l ≤ zone_high and h ≥ zone_low` |
| 2 | trigger_patterns | CLUSTER_ENGULF رنگِ کندل را چک نمی‌کرد. یک بیسِ قرمز بعد از کشیدگیِ سبز هم «انگالفِ صعودی» شناخته می‌شد. | رنگ‌های مخالف و حداقل بدنه |
| 3 | trigger_patterns | کندلِ MTF از bin ناقص (کندلِ هنوز بسته‌نشده) ساخته می‌شد. | فقط binهای کامل با origin=epoch |
| 4 | trigger_patterns | CLUSTER_BOS فقط با یک کندلِ قبلی مقایسه می‌شد. | با سوئینگِ قبلی (≥۳ کندل) |
| 5 | mtf_candles | در کندلِ هفتگی، هفته از روزِ شروعِ داده حساب می‌شد (نه دوشنبه). binهای 3D با جابه‌جاییِ پنجره جابه‌جا می‌شدند و bin ناقص هم «بسته» حساب می‌شد. | `W-MON` با left/left، 3D با epoch، حذفِ bin ناقص |
| 6 | mtf_candles | دوجیِ پایه‌بلند هم‌زمان «پین‌بار صعودی و نزولی» نامیده می‌شد. | فتیله‌ی مخالف ≤۳۰٪ رنج |
| 7 | mtf_candles | متنِ «قابل تفسیر برای سناریوی لانگ» حتی برای Bearish Engulfing زیرِ یک LONG نمایش داده می‌شد. | bias کندل محاسبه می‌شود. متن بسته به bias «خلاف سناریو — هشدار» یا «بی‌طرف» است. |
| — | patterns.py | `classify` و `measured_target` این ماژول در production استفاده نمی‌شوند (کدِ مرده). | بدون تغییر |

## ۵) کلیدها
- `R317_LEGACY=1` تغییرهای رفتاری را خاموش می‌کند: P1، P6، TB1، T5، T1 و T1b.
- اصلاحاتِ خالص (چارت، کندل‌ها، P2–P5، T4) کلید خاموشی ندارند.
- `TC_FRESH_BARS` (پیش‌فرض ۴) و `TC_MAX_BEYOND_ATR` (پیش‌فرض ۱.۵) پارامترهای قابل تنظیمِ P1 هستند.
- تست‌ها: `tests/test_audit_0925b.py` با ۱۷ تست. کلِ مجموعه: 498 passed.

## ۶) تصاویرِ قبل و بعد (کندلِ واقعی BTC)
- `docs/img/audit_0925b/1_before_1d_floating_line_stale_break.png`: خطِ معلق، دایره‌ی چسبیده به لبه‌ی چپ، و شکستِ ۱۶ روز پیش.
- `…/2_after_C1_line_anchored.png`: خط از پیوتِ واقعی عبور می‌کند. این آلارم با TB1 دیگر ساخته نمی‌شود.
- `…/3_before_15m_duplicate_lines.png` و `…/4_after_C4_dedup.png`: حذفِ خطِ تکراری
- `…/5_after_fresh_techclassic_break.png`: نمونه‌ی TECHCLASSIC که فقط روی شکستِ تازه صادر شده است.
