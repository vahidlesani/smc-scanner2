# 📌 WORKLOG — پیوستگی کار (Viva ↔ Agent)
> **قانون «ادامه بده»:** قبل از هر کاری این فایل خوانده می‌شود. کار جدید هرگز از اول شروع نمی‌شود؛
> فقط از «صف بعدی» ادامه داده می‌شود. کد سالمِ کامیت‌شده بازنویسی نمی‌شود.
> بعد از هر تسکِ تمام‌شده، همین فایل + `handoff/HANDOFF.md` + `handoff/KARTABEL.md` به‌روز و پوش می‌شوند.

---

## 📍 وضعیت لحظهٔ آخرین به‌روزرسانی
| مورد | مقدار |
|---|---|
| شاخهٔ فعال | `main` — audit patch آمادهٔ commit روی HEAD `754f4d3` |
| لایو | ✅ دیپلوی `dbe39015` **SUCCESS** + لاگ تمیز (هارت‌بیت ۲۱:۲۵Z، اسکن ۴۴ نماد، صفر exception) |
| تست‌ها | ✅ `375 passed, 1 skipped`; وب‌اپ `7 passed` |
| هندآف کامل | `handoff/HANDOFF.md` · `handoff/KARTABEL.md` · `handoff/PROJECT_MAP.md` (همه داخل همین ریپو) |
| توکن‌ها | GitHub OAuth فعال؛ Railway در این تسک متصل نیست |

## ✅ ممیزی فاز اول (۰۹-۲۳)
- قانون تأیید اکنون strictly-after-alert است؛ کندل هشدار و fallback تاریخی دیگر نمی‌توانند تأیید بسازند.
- ATR confirmation در هر فریم از True Range واقعی محاسبه می‌شود و projection خط روند به بازهٔ anchorها clamp می‌شود.
- NameErrorهای `dist`، `set_json` و `CHAT_ID_SPOT` رفع شد؛ CI pytest کامل و lint هستهٔ معاملات را اجرا می‌کند.
- UI موجود با همان chart engine ربات راستی‌آزمایی کرد: **۷ تست سبز**. دیپلوی Railway هنوز انجام نشده است.

## ✅ فاز دوم — کالیبراسیون موتور ترند و الگو (۰۹-۲۳)
- هر edge رویداد اکنون `pattern_type`, `pattern_bias`, `break_edge`, `break_direction`, `trade_direction` و `direction_reason` دارد.
- قانون ماهیت wedge حفظ و سخت‌گیر شد: rising wedge فقط شکست کف/SHORT و falling wedge فقط شکست سقف/LONG؛ edge نامعتبر Flag پس از relabel دیگر event تولید نمی‌کند.
- confirmation از قرارداد detector دفاع می‌کند؛ SHORT روی UP break و LONG روی DOWN break با `BREAK_SIDE_MISMATCH` رد می‌شوند، حتی اگر generic/internal lane تلاش به تأیید کند.
- تست نهایی: **376 passed / 1 skipped**. warningهای pandas در fixtureهای قدیمی باقی مانده‌اند و blocker عملکردی نیستند.

## ✅ فاز سوم — internal geometry، snapshot و چارت (۰۹-۲۳)
- لاین INTERNAL فقط برای `RANGE`, `RECTANGLE`, `CHANNEL` و `CHANNEL_*` مجاز شد؛ وج‌ها، مثلث‌ها و broadening دیگر از وسط یا انتهای خود internal confirmation نمی‌گیرند.
- لبهٔ داخلی از ۳۰٪ به ۲۰٪ عرض الگو سخت‌تر شد؛ entry همان close تأیید است، stop پشت کف/سقف ساختاری و target مسیر تا دیوارهٔ مقابل است.
- absorb برای زنجیرهٔ منتشرشده دیگر `entry/SL/TP` را از fresh scan کپی نمی‌کند؛ geometry همان signal ID ثابت می‌ماند و شناسهٔ جدید geometry خودش را دارد.
- لیبل‌های `LONG POSITION`/`SHORT POSITION` و level pills کنار ابزار حذف شدند؛ خطوط Entry/Stop/TP باقی هستند و مقادیر در متن پیام می‌مانند.
- راستی‌آزمایی نهایی: **377 passed / 1 skipped**.

## ✅ انجام‌شدهٔ راند ۱۶ (کامیت‌شده — تکرار نمی‌شود)
1. **فاز ۱** `2546437` — لَدر هشدار اسپات: TOUCH (برخورد اولیه) → NEAR_BREAK (نزدیک شکست) → BREAK_DOWN (شکست نزولی = فقط هشدار) → تأیید همان قانون یک‌کلوز صعودی · باکس سبز از اولین هشدار تا سقف ساختاری +۱٪ · گیت ضدتکرار سه‌مرحله‌ای (ارتقا همیشه مجاز، تکرار با کول‌داون، مارکر فقط بعد از ارسال موفق) · بودجهٔ مستقل `SPOT_ALERT_MAX_PER_DAY` (۸/روز).
2. **فاز ۲** `c876965` — جداسازی کامل اسپات از فیوچرز: ستاپ `SPOTBREAK` · تریگرهای ویوا `4h/8h/12h/1d/3d` (هفتگی حذف) · شناسهٔ یکتای `VIVA-SPOT-E000000` · سقف استاپ اسپات = ۱۰٪ (مستقل از جدول فیوچرز) · لگاریتم همه‌گیر با گارد (دامنهٔ <۳٪ → خطی) · حذف خطوط تمام‌عرض RANGE («خط طوسی») · حذف ردیف‌های NaN («چپ خالی») · لنگر ابزار روی کندل لایوِ تأیید · لیبل `VIVA-SPOT-MON` در پیام و چارت · ابطال پوزیشن + حداکثر ضرر نوشتاری در پیام تأیید اسپات · دلایل دوهخطی هشدار (معنی الگو + شاهد حجم ۲۰کندله).
3. **آشتی دو پیاده‌سازی موازی ۲د (۰۹-۲۳):** یک جلسهٔ موازی هم نسخه‌ای با «والیوم همپوشان داخل پنل قیمت» پوش کرده بود (`1dc8b7d`). در merge، چیدمانِ **والیوم جدول جدا در پایین** (منطبق با همهٔ چارت‌های مرجع ویوا: CryptoCove و اسکرین‌شات‌های خودش) نگه داشته شد + فیکس تیک‌های علمی لگاریتم (در نسخهٔ موازی غایب بود) + استیکر کمی بزرگ‌تر از آن نسخه گرفته شد.
3. **راند ۱۹ — دپلوی:** `d8d5d872` SUCCESS، لاگ پاک (هارت‌بیت ۱۴:۰۵Z، اسکن ۴۴ نماد)، اپ روی دامنه سالم (فید ۶۰/زنجیره ۲۰/برخورد ۴۰/فعال ۵/آرشیو ۱۴).
3. **راند ۱۹ (۰۹-۲۳ — آغاز «تا انتها»): فاز-۳ + دو پیرایش چارت:** ① **فیت لگاریتمی خطوط پیوت** (`_pivot_line_fit`): محور لگ + span>۳٪ → فیت در log10 (خط از log10(y) می‌گذرد؛ بازگشت mode+slope+intercept؛ هر دو سایت VALID UPPER/LOWER و 2-PIVOT WATCH وصل شدند؛ فرم‌های رسم با `_fy9/_fx9`) — ۳ تست جدید `test_phase3_log_fit.py` (دقت عبور از پیوت‌ها، گارد <۳٪/محور خطی، سیم‌کشی دو سایت) ② لیبل باکس سبز: **باکسِ بریده‌شده از سقف، پایین کشیده می‌شود** (re-anchor تا کلاً داخل پنل بماند — «باکس نصفش رو نزن») + لیبل همیشه داخل پنل ③ رجیستری اشغال چیپ‌های ناحیه (`_chip_taken`): چیپ FLIP دیگر روی DEMAND POI نمی‌نشیند. تست‌ها: **۳۵۹ سبز**. (دو تست round15 که شبکه‌روون بودند در راند ۱۸ هرمتیک شدند.)
3. **راند ۱۸ — دپلوی و راستی‌آزمایی (۰۹-۲۳):** دیپلوی `7b78c133` SUCCESS، لاگ پاک (لایسنس‌گیت هم فعال دیده شد: «ADAUSDT/PINVAL فقط ۱٫۱۵٪ از کانفیرمد قبلی» = دقیقاً قانون ۳ لایسنس/فاصلهٔ ۲٪ در عمل). تست بیرونی: جزئیات APTUSDT (تایم‌لاین+خلاصه) و **چارت واقعی 3600×2040** از `/app/api/chart/<sid>` — همان موتور چارت تلگرام، ۲۰۰ و ۴۳۴KB. شناسه‌ها فاصله دارند → سمت JS encodeURIComponent هست (تست curl خام بدون انکود شکست می‌خورد — مسیر درست).
3. **راند ۱۸ (۰۹-۲۳ — نقد اولیهٔ ویوا از اپ → حرفه‌ای‌سازی):** ① **منوی کشویی حرفه‌ای** (استایل Binance/TV: drawer راست‌گرد با هدر پروفایل، آیکون، وضعیت موتور، خروج) + ناوبار پایین ۴تایی با بج تعداد برخوردها ② **صفحهٔ جدا برای هر سیگنال**: تپ روی کارت → چارتِ واقعیِ ربات (رندر زنده با generate_chart از ردیف signals؛ کاندیدای بازسازی‌شده `_candidate_from_row`؛ کش ۹۰ثانیه‌ای) + توضیحات + شرط ورود + تأییدها + نردبان TP با تیک هیت + **تایم‌لاین کامل از signal_telegram_events** ③ **تب «اعلان برخوردها»** (TP1/TP2/استاپ/برد/باخت/تأیید — از tp1_hit_at/closed_at/confirmed_at) ④ **ستاپ‌های خاموش از آمار جدا شدند**: فقط ستاپ‌های ≤۳۰روز فعال در بورد اصلی؛ بقیه در `<details>` آرشیو با برچسب (رفع «چرا آمار کلی واسه ستاپ دوماهه خاموش؟») ⑤ فیکس تست‌های شبکه‌وابستهٔ round15 (گیت ضدروند از venue فال‌بک می‌گرفت و تست می‌شکست → monkeypatch hermetic). نکتهٔ فنی: `candidate.market` دیکشنری است (`{"asset_class":"CRYPTO","venue":"BYBIT"}`) نه رشته. ۳۵۶ تست سبز.
3. **راند ۱۷ب — دپلوی و تحویل (۰۹-۲۳):** دامنهٔ زنده: `https://smc-scanner2-production.up.railway.app` (رمز: متغیر `VIVA_APP_PASSWORD` — در ریپو نیست) · دیپلوی‌ها: `5a30cade` (اپ+قفل) → `1e481495` (فیکس زنجیره‌ها: ایمپورت از candidate_store + پوزیشن‌های لایو بالای لیست + اسپات از source=SPOTBREAK) → `84205d35` (اسپات از market_json) — همه SUCCESS، لاگ پاک · راستی‌آزمایی بیرونی: health 200/قفل 302/لاگین 200/state واقعی (فید ۶۰، زنجیره ۳۱، ۱۹ ستاپ، WR کل ۵۸٫۸٪ روی ۱۹۵۵ سیگنال) · **APK اندروید ساخته شد** (PWABuilder cloud): `/home/user/viva-android/` = apk+aab+signing.keystore+assetlinks.json (keystore pass در همان فولدر — برای گوگل‌پلی نگه داشته شود) · اسپات در داشبورد فعلاً صفر است چون هنوز رکورد اسپاتی در جدول signals ثبت نشده (موتور اسپات تازه دیپلوی شده) — با اولین سیگنال اسپات پُر می‌شود.
3. **راند ۱۷ب (۰۹-۲۳ — اپلیکیشن موبایل VIVA SIGNALS PRO):** وب‌اپ PWA تک‌فایلی (`webapp_viva.py`) + قفل واحد روی کل سرور (رمز `VIVA_APP_PASSWORD`؛ بدون رمز fail-closed، فقط /health باز) · فید زنده (کارت‌های هم‌زبانِ چارت: بج ستاپ، چیپ جهت/اسپات، پیل‌های ورود/استاپ/TP1/TP2 با رنگ‌های قانونی، چیپ نتیجه) · زنجیره‌های رصد فعال · داشبورد وین‌ریت هر ستاپ (WR/برد/باخت/میانگین-بهترین-بدترین PnL/اسکور + اسپات مقابل فیوچرز از `public_code LIKE 'VIVA-SPOT-%'`) · **کنترل**: توقف کل انتشار + کلید هر ستاپ (`bot_kv['webapp_control']`، گیت fail-open در دیسکاوری قبل از رزرو کد، اعمال ≤۵ ثانیه) · PWA کامل (manifest+SW+آیکون‌های لوگو+فونت وزیرمتن) → آیفون = Add to Home Screen · دامنهٔ Railway `smc-scanner2-production.up.railway.app` (serviceDomainCreate پورت ۸۰۸۰) · APK اندروید = TWA (راند بعد). دموی سندباکس با `VIVA_APP_DEMO=1`. ۳۵۴ تست سبز.
3. **راند ۱۷الف (۰۹-۲۳ — لیبل‌های لایو چارت + شفافیت آپدیت):** ① قیمت لایو = تگ تلویزیونی روی **نردبان قیمت** (هیچ چیپ شناور وسط چارت — نه در پیش‌نمایش نه در ستون تأییدی) ② **مهر تیرهٔ لایو زیر کندل آخر** («تاریخ و ساعت مشخص باشه که بفهمم لایوه یا قدیمیه») — سطح فیگور بعد از قطعی xlim، هیچ‌چیز دفنش نمی‌کند ③ محور زمان = تاریخ واقعی UTC (تیک‌های سرِ روز + دو‌خطی تاریخ/ساعت، بدون تیکِ کندل آخر) ④ حجم: فرمتر K/M بدون «10⁶» (offset داخل ylabelِ mplfinance بود!) ⑤ کپشن همهٔ آپدیت‌ها: «📊 چارت پیوست: لایو — تا کندلِ جاری … UTC» (رفع سوءتعبیر «چارت مال ۶ ساعت قبله» — کندل مبدا = مبدأ سیگنال است نه سن چارت). پنجره‌های fetch تأیید (_tf_fetch_window) بازبینی شد: پوشش بعد از کلوز هر TF درست است؛ تأییدِ دیرهنگامِ مشاهده‌شده در لاگ production پیگیری می‌شود. ۳۴۹ تست سبز.
3. **فاز ۲د (۰۹-۲۳ شب — قاتل واقعی «یک‌سوم خالی» اعدام شد):** تابع `_render_corner_notes` از جلسات خراب، به‌ازای هر رندر **۱۱٫۵٪ عرض پنل را می‌برید** و چندبار صدا زده می‌شد (اثر تجمعی ≈ ۲۰٪+) — همان «۶ کلمه، یک‌سوم کندل». بازنویسی کامل: **هیچ چیدمانی عوض نمی‌شود**؛ نوت‌ها داخل محور در گوشهٔ خالیِ آسما (مثل چارت مرجع ویوا) با چیپ نیمه‌شفاف. + پنل تمام‌عرض (۴٪→۸۸٫۵٪، پیکسل‌سنجی: ۲۸٪→۴٪) · نردبان قیمت ته راست · حاشیهٔ راست فقط ۲۸/۲۶ · نسبت ۱۶:۹ برگشت · تیک‌های علمی لگاریتمی حذف · **فیوچرز هم در صورت نیاز لگاریتمی** (حکم نهایی ۰۹-۲۳: لگاریتم هرجا اثر دارد؛ گارد <۳٪). رندر کنترلی چشمی + پیکسل‌سنجی + ۳۴۹ تست سبز.
3. **فاز ۲ج (۰۹-۲۳ — ریشهٔ واقعی «یک‌سوم خالی» پیدا و بسته شد):** حاشیهٔ رزروِ سمت راست چارت (۴۰–۴۲ واحد خالی) دقیقاً همان ناحیهٔ خالیِ مورد اعتراض ویوا بود → **نصف شد (۲۴/۲۲) و ۱۴ کندل واقعی جای آن آمد** (پنجرهٔ رندر ۱۵۰→۱۶۴) + گارد `_clean_render_frame` (حذف ردیف‌های NaN/صفر، حفظ قرارداد ایندکس). رندر کنترلی چشمی: کندل‌ها تا لبهٔ چپ، پیل‌ها چسبیده به کندل آخر، خالی حذف شد.
3. **فاز ۲ب** — سطل کانال ۸h/۱۲h → خانوادهٔ میانی (`2H_4H`) · معافیت اسپات از گارد استاپ فیوچرز (`_final_stop_guard`) — باگ نیمه‌کارهٔ جلسهٔ قطع‌شدهٔ سوم بسته شد.
4. **merge + دیپلوی + راستی‌آزمایی (با فرمان ویوا):** main `841202e` → تریگر `serviceInstanceDeploy` → BUILDING→**SUCCESS** (`dbe39015`) → لاگ: مانیتورها فعال، هارت‌بیت، اسکن ۴۴ نماد، **صفر exception**. طبق قانون ویوا هیچ‌وقت «دیپلوی شده» فرض نمی‌شود؛ همیشه: وضعیت دیپلوی + لاگ زنده چک می‌شود.

## ✅ راند ۱۶ فاز ۳ (۰۹-۲۳ شب — لِین مایک، کامیت `460a45c`) — لایو شد
- **کالیبراسیون لگاریتمی خطوط الگو/روند (ریشهٔ «خط لنگ در هوا») بسته شد.** روی پنجره‌ای که دامنه‌اش از ۳٪ بیشتر است چارت لگاریتمی رسم می‌شود؛ خطی که در فضای قیمت فیت شده، در آن فضا منحنی است — پس خط مستقیمِ کشیده‌شده بین دو پیوت از پیوت‌های وسط جدا می‌افتاد (اندازه‌گیری‌شده تا ۲.۱۴٪ قیمت روی ریل واقعی DOGEUSDT روزانه).
  - `analysis/viva_tlbreak.py`: `fit_validated_line` گزینهٔ `log_fit_min_span` گرفت (پیش‌فرض ۰.۰۳ هم‌سان گارد محور؛ env `TLBREAK_LOG_FIT_MIN_SPAN`). `ValidatedLine` سه فیلد `log_fit/log_slope/log_intercept` گرفت؛ `slope/intercept` همان تانژانت محلیِ آخرین تاچ می‌ماند تا همهٔ مصرف‌کننده‌های قدیمی (علامت شیب، رانش ATR، تصویر وتر) بی‌تغییر کار کنند؛ `price_at()/tangent_at()` مقدار کالیبره می‌دهند.
  - `analysis/render_kit.py`: `line_y/line_tangent/line_xy` تنها راهِ خواندن هندسهٔ یک خط سریالایز‌شده شدند (منحنیِ لاگ یا همان پاره‌خط دو‌نقطه‌ای قبلی). `detect_patterns` پارامتر `log_axis` گرفت و **خودش تصمیم چارت** را از `_chart_will_be_log` می‌گیرد: اسپات همیشه لاگ، فیوچرز فقط وقتی گارد >۳٪ اجازه بدهد → ظاهر چارت خطی فیوچرز دست‌نخورده.
  - `bot/messages_v7.py`: رسم خطوط الگو/تصویر خط‌چین از `line_xy` می‌گذرد؛ باکس «خط تخت» مقدار کالیبره می‌گیرد؛ بالا رفتن TF نمایش، خطوط لاگ را در فضای لاگ ری‌اسکیل می‌کند. (این مکملِ `_pivot_line_fit` است که همان روز برای خطوط دو‌پیوتی اضافه شده بود — نه تکراری.)
  - `analysis/quality_engine.py`: کانتینمنت داخل الگو لبه‌های لاگ را روی منحنی خودشان پروجکت می‌کند و قانون «سمت شکست» از `_project_watch_level` (لاگ‌دقیق، وگرنه همان وتر) می‌خواند.
  - `analysis/spot_engine.py`: سه نقطهٔ لبه (upper edge، کف ساختاری TP، لبه‌های نردبان) از اکسسور کالیبره می‌خوانند.
  - تست: `tests/test_round16_log_calibration.py` (۱۳). پروف: `/home/user/proof_phase3_loglines.png` (چارت A/B + پنل هندسه + کپشن فارسی). دیپلوی `460a45c4` SUCCESS · لاگ تمیز · اسکن ۴۷ نماد.
- **راند ۱۶ آیتم ۳ — ماژول آن‌چین/فلوی رایگان (`analysis/onchain_free.py`) اضافه شد (اولویت‌دهی، هرگز گیت).** منابع بی‌کلید و در دسترس: CoinGecko (چرخش volume/mcap هر نماد)، alternative.me (ترس/طمع)، DefiLlama (جزر و مد حجم DEX). کاملاً fail-open + کش (۳۰/۱۰ دقیقه) + سوییچ `ONCHAIN_FREE_ENABLED`.
  - تنها جای اثرگذاری: **ترتیب** لاین اسپات (`run_spot_scan`) که بودجهٔ نماد محدود دارد — نمادهای دارای هشدار اول، سرِ واچ‌لیست سرِ جایش (۶ نماد) و بقیه بر اساس چرخش ۲۴ساعته. صف دیسکاوری فیوچرز **دست نخورد** (فقط یک خط لاگ زمینه).
  - تست: `tests/test_round16_onchain_free_priority.py` (۱۴ — شامل تست‌های «صف فیوچرز بازچینی نشود»).
- تست کل روی این HEAD: **۴۰۴ سبز / ۱ اسکیپ**.

## 🔜 صف بعدی (به همین ترتیب — از اول شروع نشود)
0. **راند ۲۲ (۰۹-۲۴ بامداد — ممیزی ۱۳ چارت + قانون وج + یخ هندسه):**
   - **گزارش ویوا «کدها چرا تغییر کرده؟؟»:** منطق سیگنال/ستاپ‌ها تغییر نکرده بود؛ ۹ ایراد چیدمان چارتِ کانفرمد + ۲ باگ دامنه (ماهیت وج، اسنپ‌شات) + اسپوتِ بی‌وضوح. همه در همین راند بسته شد.
   - **بهداشت ستون برچسب‌ها (root cause: merge سه‌درصدی → مگاچیپ‌های سرریز + ylimِ نیمه‌کاره وسط رندر → «قرص در آسمان»):** معماری تازه — `_right_slots/_right_specs` registry؛ قرص‌های ENTRY/TP1..5/STOP/LIVE **بعد از قطعی‌شدن ylim** با `_slot_alloc` (گام 4.2٪ اسپن) ساخته می‌شوند؛ **یک قرص به‌ازای هر سطح** (مگاچیپ ممنوع) + `_level_tag` opaque (خط‌چینِ الگو دیگر روی متن نمی‌کشد) + **گارد ضدسرریز**: عرض پنل تا جایی باز می‌شود که هیچ چیزی به نردبان قیمت نرسد (باند +30) + LIVE دیگر روی اعداد محور نیست (anchor x=1.0 ممنوع در سورس — تست) + چیپ نام الگو داخل قاب و opaque + چیپ‌های ناحیه opaque.
   - **لجر گوشه (FIRST STOP/SWING/RANGE/...):** `_render_corner_notes` حالا با سنجش «رسایش کندل‌های چپ» بین سه لنگر (بالا/پایین/میانِ چپ) جایی را برمی‌گزیند که نوار واقعاً خالی است (باگ BTC/RENDER: لجر روی کندل‌های 09-22) + پس‌زمینهٔ opaque.
   - **واترمارک دوبله:** مارک برندِ داخلِ محورِ پایین-راست حذف شد (فوتر فیگور + واترمارک مرکزی کافی است).
   - **حکم ماهیت وج (کاردانو):** `_EDGE_RULES`: WEDGE_RISING فقط lower→SHORT، WEDGE_FALLING فقط upper→LONG؛ لبهٔ خلاف‌ماهیت قانون ندارد + گارد شیب هم‌علامت در دتکتور لگاسی TLBREAK و `detect_viva_tlbreak`. فید داخلی فقط کانال موازی.
   - **یخ‌زدن هندسهٔ زنجیرهٔ عمومی:** `absorb_update_into_chain` دیگر روی زنجیرهٔ PUBLIC نقاط لنگر ترندلاین/وج/خط major/retest را از اسکن تازه کپی نمی‌کند (باگ «خط کش اومده باز» و ترند جدید 14:45 با خط جابه‌جا).
   - **اسپوت دیدنی:** `_spot_status_write` در KV + ردیف «لاین اسپات» در اپ (وضعیت/آمار پاس). CHAT_ID_SPOT ست نبود → در اپ مشخص می‌شود.
   - رندر کنترلی سه‌گانهٔ چشمی: `/home/user/chart_r22_short_wedge.png` · `chart_r22_long_trend.png` · `chart_r22_short_chan.png` — ستون پاکه، یک قرص/سطح، لجر خالی، بدون واترمارک دوبله. تست: `test_round21_pattern_nature.py` (۹). کل: **۳۷۵ سبز**.
0. **راند ۲۱ (۰۹-۲۴ صبح — اعتراض ویوا به کاردانو + خط کش‌آمده + اسپات):**
   - **حکم ماهیت الگو (بسته شد):** «رایزینگ‌وج ماهیت نزولی داره — با شکست کف و کلوز زیرش تأیید میشه؛ سیگنال داخلی فقط داخل کانال و مستطیل (کف→سقف / سقف→کف)». پیاده‌سازی: `_EDGE_RULES` تکنوکلاسیک حالا WEDGE_RISING فقط lower→SHORT و WEDGE_FALLING فقط upper→LONG دارد (لبهٔ خلاف‌ماهیت اصلا قانون ندارد) + گارد ماهیت در دتکتور لگاسی TLBREAK (شیب‌های هم‌علامت) و در `detect_viva_tlbreak`. فِیدِ داخلی همچنان فقط کانال موازی (`is_parallel`). ریشهٔ باگ کاردانو: LONGِ تهِ وجِ صعودی از قوانین عمومی لبه عبور می‌کرد.
   - **اسنپ‌شات هندسه (بسته شد):** ریشهٔ «خط کش اومده باز» = `absorb_update_into_chain` همهٔ متادیتای اسکن تازه را روی زنجیرهٔ زنده کپی می‌کرد — شامل نقاط لنگر ترندلاین (هر اسکن ریفیت جدید). حالا وقتی زنجیره PUBLIC است (هشدار/پیام رفته یا کانفرمد)، کلیدهای هندسهٔ رسم (tl_a/b، نقاط viva، خط اصلی major، retest zone، tc_base/projection، watch line) یخ می‌زنند — زمینهٔ بازار تازه می‌شود، ساختارِ رسم‌شده هرگز.
   - **لاین اسپات دیدنی شد:** `_spot_status_write` وضعیت واقعی (فعال/بدون CHAT_ID_SPOT/خاموش/خطا + آمار آخرین پاس) را در KV می‌نویسد؛ اپ در کشوی کنترل «لاین اسپات» را نشان می‌دهد. ⚠️ اگر روی پروداکشن «بدون کانال اسپوت» دیدی یعنی CHAT_ID_SPOT در متغیرهای ریلوی ست نیست — همان لحظه در اپ مشخص می‌شود.
   - تست: `tests/test_round21_pattern_nature.py` (۹ تست). کل: **۳۷۵ سبز**. دیپلوی: باید راستی‌آزمایی شود (وضعیت + لاگ + چیپ اسپات در اپ).
0. **راند ۲۰/۱۹ (۰۹-۲۳ — قانون تایم‌پایین + آینهٔ اپ) — کد کامل، تست ۳۶۶ سبز، آمادهٔ کامیت/دیپلوی:**
   - **قانون استاپ/تی‌پی تایم‌پایین (HYPEUSDT):** جدول سقف استاپ جدید `{1m:1.25,3m:1.25,5m:1.50,15m:2.00,30m:2.25,1h:2.75,2h:3.25,4h:4.50,1d:8.00}` + `ltf_for_trigger` (1d→4h، 4h/2h→1h، 1h→15m، 30m/15m→5m، ≤5m→1m) + `ltf_structural_stop` (کف/سقف ۱۲ کندل LTF ±0.10%) + `ltf_tp1` (نزدیک‌ترین سوئینگ LTF ≥1.2%، سقف per-TF) — در pattern_engine TECHCLASSIC و **build_ladder/send_confirmed** (پیل اول نردبان کانفرمد هم از سوئینگ تایم‌پایین؛ پذیرش تا ۵۰٪ مسیر) + جدول مشترک `TP1_CAP_BY_TF` {1d:6.0,4h:3.5,2h:2.5,1h:2.0,30m:1.5,15m:1.2,≤5m:1.0}. همیشه fail-open.
   - **قانون ورود (MAJOR TL):** pattern_engine خط روند اصلی مخالف (بالاترین TF از `_htf_lines`) را در `viva_major_break_line` ذخیره می‌کند؛ `evaluate_confirmation` لبهٔ تأیید را تا خطِ اصلی می‌برد (اگر جلوتر باشد و داخل ۴٪/۲٫۵ATR) — تأیید = کلوز فراتر از خط اصلی، نه فقط خط ابزار. `confirm_edge_source=MAJOR_TL`.
   - **آینهٔ اپ = دقیقاً تلگرام:** `_post_chart_then_text` فایل‌آیدی چارت و HTML پیام (مختصر/کانفرمد/آپدیت) را در KV (`app_chart|sid`، `app_msg|sid|kind`) می‌نویسد؛ اپ چارت را با getFile از CDN تلگرام می‌خواند (کش ۳۰ دقیقه) — **صفر رندر دوباره** (حکم هزینهٔ ریلوی). detail اپ: «رویدادهای قیمتی» (TP1/TP2/STOP با تیک + ساعت تهران از tp1_hit_at/target_state/closed_at) + «پیام مختصر/کانفرمد (همان پیام کانال)». فیلتر لایو: `closed_at IS NULL` (سیگنال بسته هرگز در «رصد زنده» نیست — حکم ۱ مهر).
   - تست جدید: `tests/test_round20_entry_ladder_law.py` (۷ تست). **با دیپلوی بعدی: راستی‌آزمایی اجباری (وضعیت+لاگ).
1. **فاز ۳ — کالیبراسیون لگاریتمی خطوط الگو:** فیت خطوط در فضای `log10` وقتی span پنجره > ۳٪ (همان گارد محور) تا روی چارت لگاریتمی، خط دقیقاً پیوت‌ها را لمس کند (ریشهٔ قطعی «خط قرمز لنگ در هوا» — خطِ خطی‌فیت روی محور لگ از قاب خارج می‌شود و شناور دیده می‌شود). سپس رندر نمونه‌های مرجع ویوا و مقایسهٔ چشمی: SOMI 3D · MANTRA 1D · ZKP 1D · BTC 4h · TAO 1W · RENDER 3D (دیتای واقعی از سرور ریلوی — sandbox به Bybit محدود است).
2. **ظرافت‌های چارت هشدار:** برخورد چیپ `FLIP` با متن `DEMAND` (سیستم CHART-8، چند-لیبلِ یک ناحیه) · کلیپ لیبل «ارزش باکس سبز» روی لبهٔ بالای پنل.
3. **دادهٔ آنچین رایگان (حکم ویوا: فقط رایگان):** ماژول fail-open با چند منبع: DefiLlama (API عمومیِ بی‌کلید) · CoinAnk (endpoint داخلی/پروکسی — رایگان با محدودیت؛ Apify ۵ اجرای رایگان/روز برای heatmap) · CoinGlass free-tier (باید زنده تست شود؛ گزارش‌ها دوپهلوست) · CoinGecko (موجود). اولویت‌دهی رصد، هرگز گیت.
4. ~~merge + دیپلوی~~ ✅ انجام شد (بالا). پایش ۳۰ دقیقه‌ای اول: کم‌خطر — لاگ اولیه تمیز بود؛ ادامهٔ پایش هشدارهای اسپات زنده.
5. پایش زندهٔ کیفیت لَدر روی `VIVA-MON-SPOT` (کول‌داون‌ها، تازگی، بودجه) + دیده‌بانِ سناریوی «۱۸→۲۸ دلار» روی نمادهای رنج‌شکن (OUT_OF_REACH عملی تایید شود).

## 📜 حکم‌های قطعی ویوا (قانونِ پروژه شد)
- تریگر روزانهٔ (۱d) فیوچرز **بماند** (۰۹-۲۲) — مصرف عملاً صفر با کشِ تا-کلوز.
- سرویس پولی آنچین **نه** — فقط مسیرهای رایگان.
- merge فاز ۲: **بله** (در انتظار فقط فرمان اجرا).
- استاپ اسپات: سقف ۱۰٪ · خط‌چین کمرنگ پشت ساختار (اگر دیده نمی‌شود، چارت خراب نشود) · عدد ابطال نوشتاری در پیام.
- قالب/فونت پیام‌های اسپات = همان خانوادهٔ پیام تفصیلی؛ هشدارها/هیت‌ها/آپدیت‌های ۵ ستاپ فیوچرز **صفر تغییر**.
- **حکم ۰۹-۲۳:** چارت‌های فیوچرز ظاهر قبلی‌شان — لگاریتم فقط اسپات (`chart_log_all=False` پس از اعتراض ویوا به تغییر ظاهر فیوچرز؛ عذر خواسته شد). «چپ خالی» ربطی به لگاریتم نداشت: حاشیهٔ رزرو بزرگ بود.

## 🔁 پروتکل پیوستگی (جواب قطع‌شدن چت)
- ویوا: فقط می‌گوید «ادامه بده» → ایجنت این فایل را می‌خواند و از «صف بعدی» ادامه می‌دهد.
- هیچ کدی که تست سبز دارد بازنویسی نمی‌شود؛ فقط patch.
- تغییر فیوچرز فقط با خواستهٔ صریح ویوا (خط قرمز).
- بعد از هر تسک: WORKLOG + handoff + push شاخه — پس هیچ جلسه‌ای هیچ‌چیز را گم می‌کند.
- کامیت‌های فقط-مستندات با پوش کد بعدی batch می‌شوند (هر پوش = بیلد ریلوی؛ صرفه‌جویی).

## 🧾 نکات ریشه‌یابی مهم (یافته‌های ماندگار)
- «خط قرمز لنگ در هوا» = خطِ الگو که در فضای خطی فیت شده ولی محور لگاریتمی است؛ دو سر خط از قاب بیرون می‌زند و شناور دیده می‌شود → راه‌حل: فاز ۳ بند ۱.
- «چپ خالی چارت» = ردیف‌های NaN ابتدای فریم (تجمیع ۳d/۱w قدیم) → dropna در generate_chart (فاز ۲، اجرا شد).
- «خط طوسی روی کندل لایو» = خطوط تمام‌عرض RANGE HIGH/LOW → حذف شد؛ فقط گزارش گوشه.
- «۵۱ آپدیت ۱۸→۲۸ دلار» = قبل از این، سناریوی تأییدنشده تا ابد منتظر برگشت قیمت می‌ماند → الان `_scenario_out_of_reach` (فاصلهٔ >۲ATR از ناحیه در جهت سناریو = بستن زنجیره با پیام شفاف) + سقف ۱۲ آپدیت + تازگی کندل (هشدار فقط کندل تازه). **پس از دیپلوی روی لایو با نمادهای رنج‌شکن پایش شود.**

## Round 23 — merge audit + BREAK-SIDE tz hole (2026-09-24)
- Audit `09bd0a1`/`460a45c`/`f93f8f2` line-by-line vs our laws. Verdict: pill-removal in
  `09bd0a1` violated the frozen-tool law (was live ~19:30–22:00Z 09-23 → the pill-less
  HBAR/DASH/UNI/AVAX/ADA/ARB/DOT charts) — already reverted in r22 (pill-law test).
  Everything else STRENGTHENS laws (alert-candle exclusion, internal-lane restriction,
  TECHCLASSIC break contract, flag edge rules, doctrine-path bug fix, log-fit line
  accuracy, onchain priority-only) — KEPT.
- REAL production hole (ours, since r12): `_project_watch_level` raised TypeError on
  tz-aware live frames vs naive anchor timestamps → silent `except: continue` → the
  BREAK-SIDE LAW never vetoed in production (BTC K211718 / ADA K962147 / RENDER-1h
  K379224 LONGs confirmed under broken support). Fix: `_tz_match` helper normalizes
  both planes (linear + log paths); silent skip now logs. tests/test_round23_law_guard.py (5).
- Suite: 409 passed / 1 skipped.

## Round 23b — pill-column relayout (2026-09-24)
- Fresh r22 renders of the 8 reported live IDs: BTC/HBAR/RAY/ADA-1h/DOT/LTC clean
  (zero-pill defect GONE, chips inside frame, LIVE off the axis) — but a residual
  fold survived in the dense cluster: ADA ENTRY×LIVE, RENDER ENTRY×TP1, TAO LIVE×chip.
- Root cause: clamp ran AFTER slot-alloc (a clamped pill landed on a taken slot)
  and chips reserved a single-slot footprint. Fix: `_relayout_pills` deterministic
  1-D parking (clamp→sort→push→ONE uniform shift; reduced step fallback) runs as a
  final guarantee over the materialized rows; chip slot step 0.056·frame-span,
  clamp-before-alloc. 50k-case fuzz: 0 violations.
- tests/test_round23_law_guard.py +3 (fuzz, ADA edge cluster, degenerate panel).
- Suite: 412 passed / 1 skipped.

## Round 23c — control-chart root cause (2026-09-24)
- Viva caught the r22 CONTROL charts: two had no pattern at all, one drew the
  trendline floating ABOVE the candles («ترند بالای چارت، کندل‌ها پایین»).
- Root cause: those controls were built with HAND-STUFFED render metadata on a
  monotone synthetic drift — not the engine. Reproduction on the same frame shows
  detect_patterns now gives NO floating line: the 09-22 DIET law (4×ATR removal
  + duplicate line drop) + pivot-anchored painting work. The defect was the
  test fixture, not the detection engine.
- Fix: make_controls.py (workspace) — pattern-SHAPED OHLC (wedge/channel/TL
  zigzag with real pivots) → enrich_render (the production metadata call) →
  generate_chart. New controls ctl_wedge_short / ctl_channel_long / ctl_trend_long:
  engine-validated lines ON the pivots, chips + pills clean.
- Upload cache: 3 duplicate screenshots removed (2 dup-pairs); 14 old scratch
  chart_*.png (4.8MB) deleted; old chart_r22_*.png controls retired.
